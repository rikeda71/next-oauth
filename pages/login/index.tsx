import { Layout } from "../../components/Layout";
import { Login } from "../../components/organisms/Login";

const LoginIndex = () => {
  return <Layout children={<Login />} />;
};

export default LoginIndex;
